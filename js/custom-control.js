/*
(function( jQuery ) {
    wp.customize.bind( 'ready', function() {
        var customize = this;
        var Control1    =   this.control('raze_skins').container.find('button');
        alert(Control1);
    } );
})( jQuery );
*/